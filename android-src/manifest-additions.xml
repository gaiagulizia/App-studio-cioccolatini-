<!--
  ============================================================
  manifest-additions.xml
  
  Aggiungi queste righe all'AndroidManifest.xml generato da
  Capacitor (android/app/src/main/AndroidManifest.xml).
  
  ① I <uses-permission> vanno PRIMA del tag <application>.
  ② Il blocco <service> va DENTRO <application>.
  ============================================================
-->

<!-- ① Permessi (prima di <application>) -->
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
<uses-permission android:name="android.permission.WAKE_LOCK" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

<!-- ② Servizio (dentro <application>) -->
<service
    android:name=".TimerService"
    android:foregroundServiceType="specialUse"
    android:exported="false">
    <property
        android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
        android:value="timer" />
</service>
